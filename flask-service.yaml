apiVersion: v1
kind: Service
metadata:
  name: flask-service
spec:
  type: LoadBalancer
  selector:
    app: flask-app
  ports:
    - name: http
      port: 8080        # Внешний порт
      targetPort: 5000  # Порт внутри контейнера Flask
